<Page
    x:Class="CookbookWin10.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CookbookWin10"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState x:Name="wideState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="720" />
                    </VisualState.StateTriggers>
                </VisualState>
                <VisualState x:Name="narrowState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="inputPanel.Orientation" Value="Vertical"/>
                        <Setter Target="inputButton.Margin" Value="0,4,0,0"/>
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <StackPanel x:Name="contentPanel" Margin="8,32,0,0">
            <StackPanel x:Name="panelDailyRecipe" Orientation="Vertical" Margin="0,20,1019,20">
                <TextBlock x:Name="lblDailyRecipe" Text="The recipe of the day:" Margin="0,0,0,50"/>
                <StackPanel HorizontalAlignment="Left" Height="243" Margin="0,0,0,0" VerticalAlignment="Top" Width="250" Background="#FF7A7373">
                    <Image x:Name="imgDailyImage" Height="243"/>
                </StackPanel>
                <TextBlock x:Name="lblDailyTitle" Text="Title" Margin="0,50,0,0"/>
                <TextBlock x:Name="lblDailyCategory" Text="Category" Margin="0,10,0,30"/>
                <Button x:Name="btnDailyNew" Content="New Recipe" Click="newDailyRecipe"/>
            </StackPanel>
            

        </StackPanel>

        <ListBox x:Name="listbox_mainlist" Margin="819,10,10,10" SelectionChanged="navigateToRecipePage">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding}" />
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Page>
